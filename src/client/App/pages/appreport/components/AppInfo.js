import React, {PropTypes} from 'react';

const AppInfo = (props) => {
    return (
        <div>APP Report App</div>
    );
};

AppInfo.propTypes = {
    //  : PropTypes.string.isRequired
};

export default AppInfo;
