import React, {PropTypes} from 'react';

const Leaderboard = (props) => {
    return (
        <div>Leaderboards index</div>
    );
};

Leaderboard.propTypes = {
    //  : PropTypes.string.isRequired
};

export default Leaderboard;
