import React, {PropTypes} from 'react';

const TopAppList = (props) => {
    return (
        <div>Top App List</div>
    );
};

TopAppList.propTypes = {
    //  : PropTypes.string.isRequired
};

export default TopAppList;
