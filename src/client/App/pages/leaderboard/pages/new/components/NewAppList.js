import React, {PropTypes} from 'react';

const NewAppList = (props) => {
    return (
        <div>New App List</div>
    );
};

NewAppList.propTypes = {
    //  : PropTypes.string.isRequired
};

export default NewAppList;
